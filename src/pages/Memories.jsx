import { useState } from "react";
import { useNavigate } from "react-router-dom";

export default function Memories() {
  const nav = useNavigate();

  // ใช้รูป 11–20.jpg
  const imgs = Array.from({ length: 10 }).map((_, i) => `/images/${i + 11}.jpg`);

  // ✅ คำคมแต่ละรูป (ไม่ซ้ำกันเลย)
  const quotes = [
    "ดอกไม้ที่เธอถือ คือความรู้สึกทั้งหมดของฉันในวันนั้น 🌷",
    "รอยยิ้มในรูปนี้ยังทำให้ใจเต้นทุกครั้งที่มอง 💗",
    "ความทรงจำของเรา... งดงามกว่าฤดูใด ๆ 🍃",
    "ไม่ต้องมีคำพูดใด ๆ แค่เธออยู่ในภาพก็เพียงพอแล้ว 💞",
    "ในทุกวันธรรมดา เธอคือเหตุผลของรอยยิ้มฉัน ☀️",
    "ภาพนี้คือวันแรกที่ฉันรู้ว่า…เราเข้ากันดีเหลือเกิน 💫",
    "เธอไม่รู้หรอก ว่าฉันมองรูปนี้ซ้ำกี่ครั้งแล้ว 💕",
    "วันที่ฟ้าครึ้ม แต่หัวใจเราสดใส 🌦️",
    "บางครั้งแค่ได้อยู่ข้าง ๆ ก็เป็นวันพิเศษแล้ว 🕊️",
    "ภาพนี้ไม่มีเสียง... แต่หัวใจยังพูดว่า ‘รัก’ อยู่เสมอ 💌",
  ];

  // เปิดภาพเต็มจอ
  const [selected, setSelected] = useState(null);

  return (
    <div className="memories">
      <header>
        <h2>Our Little Moments</h2>
        <p>เรื่องราวของเราที่อยากเก็บไว้เสมอ 💞</p>
      </header>

      {/* แกลเลอรีรูปภาพ */}
      <div className="gallery">
        {imgs.map((src, i) => (
          <figure
            key={i}
            className="card polaroid-plain"
            onClick={() => setSelected(src)}
          >
            <img src={src} alt={`memory-${i}`} />
            <figcaption>💗 {quotes[i]}</figcaption>
          </figure>
        ))}
      </div>

      <div className="center mt">
        <button className="btn" onClick={() => nav("/thank-you")}>
          Next →
        </button>
      </div>

      {/* Lightbox แสดงภาพเต็มจอ */}
      {selected && (
        <div className="lightbox" onClick={() => setSelected(null)}>
          <img src={selected} alt="full-memory" className="lightbox-img" />
        </div>
      )}
    </div>
  );
}
